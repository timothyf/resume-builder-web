<%= form_with(model: layout_section) do |form| %>
  <% if layout_section.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(layout_section.errors.count, "error") %> prohibited this layout_section from being saved:</h2>

      <ul>
        <% layout_section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
